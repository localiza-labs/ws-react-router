import styled from "styled-components";

export const ConfirmationCode = styled.div`
  color: var(--color-primary);
  font-size: 24px;
  text-transform: uppercase;
  padding: 18px 88px;
  border: 2px dashed #D7D7D7;
`;
